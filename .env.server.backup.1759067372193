# 服务器环境变量配置
# CORS 允许的域名列表（用逗号分隔）
# 开发环境
CORS_ORIGINS=http://localhost:3456,http://localhost:3457,http://localhost:3000,http://localhost:3001

# 生产环境（取消注释并修改为你的实际域名）
# CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# 服务器端口
PORT=3457

# Node 环境
NODE_ENV=production

# API基础URL（用于生成完整URL）
API_BASE_URL=https://your-domain.com